<!-- The Connoriginal Timeline -->
<ul class="connoriginal-timeline">
  {% for content in entry.contents %}
    {% assign is_even = forloop.index0 | modulo: 2 %}
    <li>
      {% if is_even == 0 %}
        <div class="connoriginal-direction-l">
          <div class="connoriginal-flag-wrapper">
            <div class="connoriginal-header">
              <div class="connoriginal-time-row">
                <span class="connoriginal-time-wrapper"
                  ><span class="connoriginal-time">{{ content.year }}</span></span
                >
              </div>
              <div class="connoriginal-logo-title-row">
                {% if content.logo %}
                  <img src="{{ '/assets/img/logos/' | append: content.logo | relative_url }}" alt="{{ content.institution }} logo" class="connoriginal-logo">
                {% endif %}
                <div class="connoriginal-title-institution">
                  <strong class="connoriginal-title">{{ content.title }}</strong>
                  <span class="connoriginal-flag">{{ content.institution }}</span>
                </div>
              </div>
            </div>
          </div>
          <div class="connoriginal-desc">
            {% if content.description %}
              {% for item in content.description %}
                {{ item }}
                {% unless forloop.last %}<br>{% endunless %}
              {% endfor %}
            {% endif %}
          </div>
        </div>
      {% else %}
        <div class="connoriginal-direction-r">
          <div class="connoriginal-flag-wrapper">
            <div class="connoriginal-header">
              <div class="connoriginal-time-row">
                <span class="connoriginal-time-wrapper"
                  ><span class="connoriginal-time">{{ content.year }}</span></span
                >
              </div>
              <div class="connoriginal-logo-title-row">
                {% if content.logo %}
                  <img src="{{ '/assets/img/logos/' | append: content.logo | relative_url }}" alt="{{ content.institution }} logo" class="connoriginal-logo">
                {% endif %}
                <div class="connoriginal-title-institution">
                  <strong class="connoriginal-title">{{ content.title }}</strong>
                  <span class="connoriginal-flag">{{ content.institution }}</span>
                </div>
              </div>
            </div>
          </div>
          <div class="connoriginal-desc">
            {% if content.description %}
              {% for item in content.description %}
                {{ item }}
                {% unless forloop.last %}<br>{% endunless %}
              {% endfor %}
            {% endif %}
          </div>
        </div>
      {% endif %}
    </li>
  {% endfor %}
</ul>
